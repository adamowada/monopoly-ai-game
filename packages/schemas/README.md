# Shared Schemas

This package is the shared schema generation area for frontend and backend contracts.

Later phases will place generated OpenAPI artifacts, TypeScript contract output, and shared value
definitions here. Stage 1.1 keeps the directory documented and empty of generated files so future
contract generation has a stable workspace path.
